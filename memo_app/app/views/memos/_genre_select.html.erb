    <%= form_with url: memos_path, method: :get, remote: true,class: "form-horizontal" do |form| %>
        <%= genre_select(form, @selected_genre_id) %>
        <%= form.submit "検索", class: "btn btn-primary" %>
    <% end %>